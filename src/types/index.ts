export type Thing = {
  id: number;
  name: string;
  username: string;
  email: string;
};

export type Nilai = {
  x: number;
  y: number;
};

export type Isi = {
  id: string;
  nama: string;
  nomer: number;
};
