export type Thing = {
  id: number;
  name: string;
  username: string;
  email: string;
};

export type Nilai = {
  x: number;
  y: number;
};

export type Isi = {
  id: string;
  Bulan: string;
  Total: number;
};

export type HistorianData = {
  DateTime: string;
  Value: number;
};

export type HistorianType={
  Value: number;
};

export type Data = {
  message: string;
  error?: string;
};
